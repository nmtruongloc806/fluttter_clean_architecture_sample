import 'package:flutter/material.dart';

class HandleDocumentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Xử lý văn bản')),
      body: Center(
        child: Text('Danh sách văn bản cần xử lý'),
      ),
    );
  }
}
