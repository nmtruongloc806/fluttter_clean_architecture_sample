import 'package:flutter/material.dart';

class FeedbackPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Xử lý phản ánh')),
      body: Center(
        child: Text('Danh sách phản ánh'),
      ),
    );
  }
}
